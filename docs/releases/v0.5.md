# 🚀 DirAgent v0.5

DirAgent 首次正式发布。目标是把“打开终端 -> 切目录 -> 启动 Agent CLI”的重复流程，压缩成文件管理器中的一次右键操作。

## ✨ 核心功能

- 目录场景右键菜单：
  - `Open in Codex (DirAgent)`
  - `Open in Claude Code (DirAgent)`
- 仅在目录/目录背景显示菜单，文件场景默认不显示
- 支持 Windows Terminal `profile` + `shell` 配置（`powershell` / `cmd` / `cmder`）
- `codex` / `claude` 默认参数可通过 `config.toml` 配置

## 📦 下载方式（Assets）

请在本页面 `Assets` 区域下载可执行文件，不要使用 `Source code (zip/tar.gz)`。

按平台选择：

- Windows x64: `diragent_v0.5_windows_amd64.exe`
- Windows ARM64: `diragent_v0.5_windows_arm64.exe`
- macOS Intel: `diragent_v0.5_darwin_amd64`
- macOS Apple Silicon: `diragent_v0.5_darwin_arm64`
- Linux x64: `diragent_v0.5_linux_amd64`
- Linux ARM64: `diragent_v0.5_linux_arm64`

## 📖 安装与配置

安装与配置请查看：

- 中文：`README.md`
- English: `README.en.md`
