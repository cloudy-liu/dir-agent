# 🚀 DirAgent v0.5

DirAgent 首个公开版本，目标是把“打开终端 -> 切目录 -> 启动 Agent CLI”的重复流程压缩为文件管理器右键一键启动。

## ✨ 本版本包含

- 目录场景右键菜单：`Open in Codex (DirAgent)` / `Open in Claude Code (DirAgent)`
- 文件右键默认不显示（减少语义歧义）
- Windows Terminal 支持 `profile` + `shell` 配置（`powershell` / `cmd` / `cmder`）
- `codex` / `claude` 默认参数可在 `config.toml` 中配置
- README 中英文重构与同步，并补充演示截图

## 📦 下载说明（Assets）

请在 Release 页的 `Assets` 区域下载二进制文件。  
不要使用 `Source code (zip/tar.gz)` 作为可执行文件。

按平台选择：

- Windows x64: `diragent_v0.5_windows_amd64.exe`
- Windows ARM64: `diragent_v0.5_windows_arm64.exe`
- macOS Intel: `diragent_v0.5_darwin_amd64`
- macOS Apple Silicon: `diragent_v0.5_darwin_arm64`
- Linux x64: `diragent_v0.5_linux_amd64`
- Linux ARM64: `diragent_v0.5_linux_arm64`

## 📖 安装指引

详细安装和配置说明请查看 README：

- 中文：`README.md`
- English: `README.en.md`
